#pragma once
#include<WinSock2.h>

class CSendAndMessageType
{
public:
	CSendAndMessageType();
	~CSendAndMessageType();
	/*const int& operator()(const Message& msg, SOCKET s,
		char *buff,
		const int& len,
		const int& flag)*/
};

